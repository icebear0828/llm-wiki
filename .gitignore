# Python / uv
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.coverage
htmlcov/

# Node (vendor/notebooklm has its own)
node_modules/

# Obsidian transient state (keep config + plugins)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# OS
.DS_Store
Thumbs.db

# Vault content (user-local; never committed to the framework repo).
# See *.example templates and docs/SETUP.md for layout.
raw/
wiki/
assets/
*.tmp
*.swp
.cookiejar_*

# Local secrets / config overrides — copy from *.example and fill in.
# CREDENTIAL CONTAINMENT: git autopilot also blocks these at stage/push time.
r2.toml
autopilot.toml
gateway.toml
imagen.toml
im.toml
.env
*.env
.env.*
config.local.toml

# Local RAG index (user cache, never committed)
.llmwiki/

# User profile (personal, not for the repo). Templates: USER.md.example.
USER.md
